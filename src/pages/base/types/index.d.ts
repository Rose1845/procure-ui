export type ContactData = {
  name: string;
  email: string;
  title: string;
  message: string;
};

export interface accordionType {
  titles: string;
  details: string;
}

export type UserProfileToken = {
  user: string;
  email: string;
  token: string;
};
